# Copied to client/.env, which is git-ignored and must stay that way. Read by the `env` extension
# in client/gradle/dotenv, whose only consumer is the kobup publish helper applied from outside
# this repository. Exporting KOBUP_TOKEN instead works too, and is the CI path.
KOBUP_TOKEN=
